% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portal.R
\name{portal_queries}
\alias{portal_queries}
\alias{portal_files}
\title{Query the IGVF Data Portal}
\usage{
portal_files(content_type, frame = "object", limit = 25L, ...)
}
\arguments{
\item{content_type}{character(1) the content type of files to
retrieve, e.g. \code{"reporter genomic variant effects"}.}

\item{frame}{character(1) \code{"object"} (default) returns full
properties with embedded objects as URI paths; \code{"embedded"}
fully expands nested objects.}

\item{limit}{integer(1) or \code{"all"} to return all matching results.
Defaults to 25L. Note the portal rate limit is 10 requests/sec.}

\item{...}{additional filter parameters passed as query parameters
to \verb{/search/}, e.g. \code{status = "released"}. Use the syntax
\code{field != "value"} filters via named arguments where needed.}
}
\value{
\code{portal_files()} returns a tibble of matching files, with
columns derived from the \verb{@graph} array of the JSON-LD response.
}
\description{
\code{portal_files()} searches for files on the IGVF Data
Portal by content type and optional additional filters. The API
is documented at \url{https://data.igvf.org/help/igvf-api-spec/}.
}
\examples{
portal_files("reporter genomic variant effects")

portal_files("reporter genomic variant effects", limit = 5L)

}
